export default interface IframeConfig {
  width?: string;
  height?: string;
  base: string;
  id: string;
  color?: string;
  auto_play?: boolean;
  hide_related?: boolean;
  show_comments?: boolean;
  show_user?: boolean;
  show_reposts?: boolean;
  show_teaser?: boolean;
  visual?: boolean;
  inverse?: boolean;
}
